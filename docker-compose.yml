services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ticketing
      POSTGRES_USER: ticketing
      POSTGRES_PASSWORD: ticketing
    ports:
      - "5432:5432"
    volumes:
      - ticketing-postgres-data:/var/lib/postgresql/data

volumes:
  ticketing-postgres-data:
